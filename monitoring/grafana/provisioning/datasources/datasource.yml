apiVersion: 1

datasources:
  - name: Prometheus
    uid: onprem-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
